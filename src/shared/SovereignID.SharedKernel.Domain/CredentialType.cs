namespace SovereignID.SharedKernel.Domain;

/// <summary>
/// Perfiles de credencial verificable admitidos en el producto (conjunto cerrado).
/// </summary>
public enum CredentialType
{
    /// <summary>
    /// Credencial académica de título de graduación (slice A).
    /// </summary>
    TituloGraduacion = 0,
}
